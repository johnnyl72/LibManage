<div class="row mt-4">
    <div class="col-4">
        <h3>Pay your bills</h3>
        <%= form_with url: fees_path do |form| %>
        <div class="form-group">
            <%= form.label :user_id %>
            <%= form.text_field :user_id, class: "form-control" %>
        </div>
        <div class="form-group">
            <%= form.label :payment_amount %>
            <%= form.number_field :payment_amount, step: 0.01, class: "form-control" %>
        </div>
        <%= form.submit "Submit", class: "btn btn-primary" %>
        <% end %>
    </div>
    <div class="col-8">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Updated</th>
                    <th scope="col">UserID</th>
                    <th scope="col">User email</th>
                    <th scope="col">Debt</th>
                </tr>
            </thead>
            <tbody>
                <% @users.each do |user| %>
                    <tr>
                        <th scope="row"><%= user.updated_at.strftime("%x %X") %></th>
                        <%= content_tag :td, user.id %>
                        <%= content_tag :td, user.email %>
                        <%= content_tag :td, number_to_currency(user.debt) %>
                    </tr>
                <% end %>
            </tbody>
        </table>
    </div>
</div>
