<div class="row">
<div class="col-3">
    <%= form_with scope: :search, url: search_path, method: :get, local: true do |form| %>
        <div class="form-group">
            <%= form.label :title %>
            <%= form.text_field :title, class: "form-control", placeholder: "Book title" %>
        </div>
        <div class="form-group">
            <%= form.label :author %>
            <%= form.text_field :author, class: "form-control", placeholder: "Book author" %>
        </div>
        <%= form.submit "Search", class: "btn btn-primary" %>
    <% end %>
</div>
<div class="col-9">
<ul class="list-group">
    <% if @results.any? %>
        <% @results.each do |result| %>
            <%= content_tag :li, "Title: #{result.title} | Author: #{result.author} | ISBN: #{result.isbn}", class: "list-group-item" %>
        <% end %>
    <% else %>
        <li class="list-group-item">NOTHING!</li>
    <% end %>
</ul>
</div>
</div>
